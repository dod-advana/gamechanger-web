import React from 'react';

const getSearchMatrixItems = (props) => {
	return <></>;
};

const DefaultSearchMatrixHandler = (props) => {
	return <>{getSearchMatrixItems(props)}</>;
};

export default DefaultSearchMatrixHandler;
